// Test file for acronym capitalization rules
// Run: npx eslint acronym-test.ts

/* eslint-disable @typescript-eslint/no-unused-vars */

// ❌ ERRORS - These should fail linting (lowercase 'Id', 'Ip', 'Api', 'Ms')
type UserId = string; // Error: should be 'UserID'
type IpAddress = string; // Error: should be 'IPAddress'
class ApiClient {} // Error: should be 'APIClient'

function getUserId(userId: string): string {
  // Error: both should be 'getUserID' and 'userID'
  const serverIp = '192.168.1.1'; // Error: should be 'serverIP'
  const apiKey = 'secret'; // OK (lowercase acronym at start)
  const htmlContent = '<div>test</div>'; // OK (lowercase acronym at start)
  const jsonData = { test: true }; // OK (lowercase acronym at start)
  const imageUrl = 'https://example.com/image.png'; // Error: should be 'imageURL'
  const timeoutMs = 5000; // Error: should be 'timeoutMS'
  const delayMs = 100; // Error: should be 'delayMS'

  return userId;
}

// ✅ CORRECT - These should pass linting
type UserID = string; // Correct
type IPAddress = string; // Correct
class APIClient {} // Correct

function getUserID(userID: string): string {
  const serverIP = '192.168.1.1'; // Correct
  const imageURL = 'https://example.com/image.png'; // Correct
  const timeoutMS = 5000; // Correct
  const delayMS = 100; // Correct

  // Variables starting with acronyms (OK to be lowercase OR uppercase)
  const apiKey = 'secret'; // OK
  const APIKey = 'secret-2'; // Also OK
  const htmlContent = '<div>test</div>'; // OK
  const HTMLContent = '<div>test2</div>'; // Also OK

  // PascalCase variables are now allowed (for React contexts, HOCs, etc.)
  const SomeRandomName = 'test'; // Correct (PascalCase allowed)
  const UserName = 'john'; // Correct (PascalCase allowed)

  // Acronyms at start (always lowercase in variables)
  const id = '123'; // Correct
  const ip = '192.168.1.1'; // Correct
  const api = 'v1'; // Correct
  const ms = 1000; // Correct

  // Multiple acronyms
  const userIDFromAPI = '123'; // Correct
  const APIToJSON = (data: unknown) => data; // Correct
  const HTMLToJSON = (html: string) => html; // Correct

  // Constants (UPPER_CASE)
  const API_KEY = 'const-key'; // Correct
  const HTML_CONTENT = '<div></div>'; // Correct
  const USER_ID = '456'; // Correct
  const TIMEOUT_MS = 3000; // Correct

  return userID;
}

// ✅ CORRECT - API interfaces with snake_case properties
interface RedirectInfo {
  target: string;
  permanent: boolean;
  preserve_query?: boolean; // Should be OK (snake_case allowed for properties)
  max_redirects?: number; // Should be OK
}

interface APIResponse {
  user_id: string; // Correct (snake_case with acronym is OK)
  userID: string; // Correct (camelCase with uppercase acronym)
  userId: string; // Error: should be 'userID' (lowercase acronym in camelCase)
  created_at: string; // Correct (snake_case allowed)
  is_active: boolean; // Correct (snake_case allowed)
}

// React component props with PascalCase properties
interface RouteErrorBoundaryProps {
  NotFoundComponent?: React.ComponentType<{ error?: unknown }>; // Correct (PascalCase for component)
  ApplicationErrorComponent?: React.ComponentType<{ error: unknown }>; // Correct (PascalCase for component)
  FallbackComponent?: React.ComponentType; // Correct (PascalCase for component)
}

// React contexts and HOCs with PascalCase variables
const UnirendContext = { test: 'value' }; // Correct (PascalCase for contexts)
const ThemeContext = { test: 'value' }; // Correct (PascalCase for contexts)
const WithAuth = () => null; // Correct (PascalCase for HOCs)

export {};
